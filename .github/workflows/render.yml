      - name: "Nuclear Injection & Smart Filter"
        shell: bash
        env:
          DYNAMIC_CODE: ${{ github.event.client_payload.tsx_code }}
        run: |
          rm -rf src/*
          touch render.log
          
          # Node Writer for raw text integrity
          node -e "const fs = require('fs'); fs.writeFileSync('src/Component.tsx', process.env.DYNAMIC_CODE);"
          
          cat <<'EOF' > src/index.tsx
          import { registerRoot, Composition } from 'remotion';
          import React from 'react';
          import * as Comp from './Component';
          
          export const RemotionRoot: React.FC = () => {
            const allExports = Object.entries(Comp);
            
            // Logic: Pehle 'HelloWorld' dhoondo, phir koi Capitalized Function, phir Default
            const Target = Comp.HelloWorld || 
                           allExports.find(([name, val]) => typeof val === 'function' && name[0] === name[0].toUpperCase())?.[1] || 
                           (Comp as any).default;
            
            if (!Target || typeof Target !== 'function') {
              throw new Error("❌ Error #130 Killer: No valid Component function found! Check your exports.");
            }

            return (
              <Composition
                id="HelloWorld"
                component={Target as any}
                durationInFrames={150}
                fps={30}
                width={1920}
                height={1080}
              />
            );
          };

          registerRoot(RemotionRoot);
          export default RemotionRoot;
          EOF
